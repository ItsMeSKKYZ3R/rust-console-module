# Example for the out function
---
### For use this function, import the module as precise in the README.md file and write it in your file
```rs
// Import the module

mod console;

fn main() {
    console::out("Some text", true /* false if you don't want a newline */);
}
```